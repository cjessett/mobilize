<%= form_with model: template, class: "contents" do |form| %>
  <% if template.errors.any? %>
    <p class="py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium rounded-lg inline-block"><%= template.errors.full_messages.to_sentence %></p>
  <% end %>
  <div class="my-5">
    <%= form.label :name, class: "block text-sm font-medium text-gray-700" %>
    <%= form.text_field :name, required: true, class: "block shadow-sm rounded-md border border-gray-400 px-3 py-2 mt-2 w-full md:w-1/2 bg-white" %>
  </div>
  <div class="my-5">
    <%= form.label :body, class: "block text-sm font-medium text-gray-700" %>
    <%= form.text_area :body, rows: 5, required: true, class: "block shadow-sm rounded-md border border-gray-400 px-3 py-2 mt-2 w-full bg-white" %>
    <p class="text-xs text-gray-400 mt-1">Merge tags: {{first_name}}, {{last_name}}, {{name}}, {{chapter_name}}.</p>
  </div>
  <%= form.submit template.persisted? ? "Save" : "Create template", class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white font-medium cursor-pointer" %>
<% end %>
