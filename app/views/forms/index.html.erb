<%= render "shared/flash" %>

<div class="flex items-center justify-between mb-4">
  <h1 class="font-bold text-3xl">Forms & petitions</h1>
  <div class="flex gap-2">
    <%= link_to "New petition", new_form_path(kind: "petition"), class: "rounded-md px-3.5 py-2 border border-slate-600 bg-slate-800/60 hover:bg-slate-700 text-sm font-medium" %>
    <%= link_to "New signup form", new_form_path, class: "rounded-md px-3.5 py-2 bg-indigo-600 hover:bg-indigo-500 text-white text-sm font-medium" %>
  </div>
</div>

<div class="bg-slate-800/60 rounded-lg border border-slate-700/70 divide-y divide-slate-700/60">
  <% if @forms.empty? %>
    <p class="p-6 text-slate-400">No forms yet. Signup forms and petitions collect people straight into your CRM.</p>
  <% end %>
  <% @forms.each do |form| %>
    <div class="p-4 flex items-center justify-between">
      <div>
        <%= link_to form.title, form, class: "font-medium text-indigo-400 hover:underline" %>
        <span class="text-xs bg-slate-700 text-slate-300 rounded-full px-2 py-0.5 ml-2"><%= form.kind %></span>
      </div>
      <span class="text-sm text-slate-500">
        <%= form.signature_count %> <%= form.petition? ? "signatures" : "submissions" %><%= " / #{form.goal} goal" if form.goal %>
      </span>
    </div>
  <% end %>
</div>
