<div class="mx-auto md:w-2/3 w-full">
  <%= render "shared/flash" %>
  <h1 class="font-bold text-3xl mb-4">Settings</h1>
  <%= render "settings/nav" %>

  <div class="flex items-center justify-between mb-4">
    <h2 class="font-semibold text-xl">Chapters</h2>
    <%= link_to "New chapter", new_settings_chapter_path, class: "rounded-md px-3.5 py-2 bg-indigo-600 hover:bg-indigo-500 text-white text-sm font-medium" %>
  </div>

  <div class="bg-slate-800/60 rounded-lg border border-slate-700/70 divide-y divide-slate-700/60">
    <% @chapters.each do |chapter| %>
      <div class="p-4 flex items-center justify-between">
        <div>
          <div class="font-medium">
            <%= chapter.name %>
            <% if chapter.default? %><span class="text-xs text-slate-500 ml-1">(default)</span><% end %>
          </div>
          <div class="text-sm text-slate-400">
            <%= chapter.phone_number.presence || "No phone number" %>
            · <%= pluralize(chapter.chapter_zip_codes.size, "zip code") %>
          </div>
        </div>
        <div class="flex gap-3">
          <%= link_to "Edit", edit_settings_chapter_path(chapter), class: "text-sm text-indigo-400 hover:underline" %>
          <% unless chapter.default? %>
            <%= button_to "Delete", settings_chapter_path(chapter), method: :delete, data: { turbo_confirm: "Delete this chapter?" }, class: "text-sm text-red-400 hover:underline cursor-pointer" %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
