<%= link_to "← All events", public_events_path(@organization.slug), class: "text-sm text-blue-700 hover:underline" %>

<h1 class="font-bold text-3xl mt-2"><%= @event.title %></h1>
<div class="text-gray-500 mt-1">
  <%= @event.starts_at.in_time_zone(@organization.time_zone).strftime("%A %b %-d, %Y %l:%M %p %Z") %>
  <%= " · #{@event.location}" if @event.location.present? %>
</div>

<% if @event.description.present? %>
  <div class="mt-4 text-gray-700 whitespace-pre-wrap"><%= @event.description %></div>
<% end %>

<div class="bg-white rounded-lg border border-gray-200 p-5 mt-6">
  <h2 class="font-semibold text-lg mb-1">RSVP</h2>
  <% if @event.full? %>
    <p class="text-sm text-yellow-700 mb-3">This event is at capacity — you'll be added to the waitlist.</p>
  <% end %>
  <%= form_with url: rsvp_public_event_path(@organization.slug, @event), method: :post, class: "contents" do |form| %>
    <div class="grid md:grid-cols-2 gap-3 mt-3">
      <%= form.text_field :first_name, placeholder: "First name", required: true, class: "shadow-sm rounded-md border border-gray-300 px-3 py-2 bg-white" %>
      <%= form.text_field :last_name, placeholder: "Last name", class: "shadow-sm rounded-md border border-gray-300 px-3 py-2 bg-white" %>
      <%= form.telephone_field :phone, placeholder: "Phone", class: "shadow-sm rounded-md border border-gray-300 px-3 py-2 bg-white" %>
      <%= form.email_field :email, placeholder: "Email", class: "shadow-sm rounded-md border border-gray-300 px-3 py-2 bg-white" %>
      <%= form.text_field :zip_code, placeholder: "Zip code", class: "shadow-sm rounded-md border border-gray-300 px-3 py-2 bg-white" %>
    </div>
    <p class="text-xs text-gray-400 mt-2">Provide a phone number or email. By RSVPing with a phone number you agree to receive event reminders by text.</p>
    <%= form.submit @event.full? ? "Join waitlist" : "RSVP", class: "mt-3 rounded-md px-4 py-2.5 bg-blue-600 hover:bg-blue-500 text-white font-medium cursor-pointer" %>
  <% end %>
</div>
